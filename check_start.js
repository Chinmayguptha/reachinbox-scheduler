
try {
    const { execSync } = require('child_process');

    console.log("Starting Backend...");
    // Start backend in background or just concurrently
    // We will use concurrently from root, but this script is for manual check if needed

} catch (e) {
    console.error(e);
}
